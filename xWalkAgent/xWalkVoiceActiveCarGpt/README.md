# xWalkVoiceActiveCarGpt

This Agent profile provides the Buddy identity, English speech and wake defaults,
speech voice, welcome text, action-format instructions, image setting, and
proximity limit. The process composition layer selects the language-model
provider, endpoint, model, and credential from deployment configuration; this
Agent does not hardcode a vendor or model name.
